#!/usr/bin/env bash


DB_PATH="/vhosts/reader/db"
DB_NAME="reader.db"

model_dir=${1-../model/db}

rm -f "$DB_PATH/$DB_NAME"

cat $model_dir/* | sqlite3 "$DB_PATH/$DB_NAME"

< subscriptions.xml bin/greader_import
